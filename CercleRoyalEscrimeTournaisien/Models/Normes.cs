using System;
using System.IO;
using System.Linq;

namespace CercleRoyalEscrimeTournaisien
{
    public class Normes
    {
        public Normes()
        {
            //string folder = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "home/photos/Ronse février 2026");
            //string[] images = Directory.GetFiles(folder).Where(f => 
            //   f.EndsWith(".jpg", StringComparison.OrdinalIgnoreCase)
            //|| f.EndsWith(".jpeg", StringComparison.OrdinalIgnoreCase) 
            //|| f.EndsWith(".png", StringComparison.OrdinalIgnoreCase) 
            //|| f.EndsWith(".gif", StringComparison.OrdinalIgnoreCase)).ToArray();
            //foreach (var img in images) { Console.WriteLine(img); }
        }
    }
}